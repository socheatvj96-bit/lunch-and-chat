server {
    listen 80;
    server_name pm.obed.pro;

    client_max_body_size 200M;
    
    # Редирект на HTTPS (будет работать после получения сертификата)
    # return 301 https://$server_name$request_uri;
    
    # Временно оставляем HTTP для получения сертификата
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    location / {
        proxy_pass http://localhost:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
    
    location /static/ {
        alias /opt/lunch_order/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    location /media/ {
        alias /opt/lunch_order/media/;
        expires 30d;
        add_header Cache-Control "public";
        access_log off;
    }
}

# HTTPS конфигурация (будет активирована после получения сертификата)
# server {
#     listen 443 ssl http2;
#     server_name pm.obed.pro;
#     
#     ssl_certificate /etc/letsencrypt/live/pm.obed.pro/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pm.obed.pro/privkey.pem;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     location / {
#         proxy_pass http://localhost:8082;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_redirect off;
#     }
#     
#     location /static/ {
#         alias /opt/lunch_order/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
#     
#     location /media/ {
#         alias /opt/lunch_order/media/;
#         expires 30d;
#         add_header Cache-Control "public";
#         access_log off;
#     }
# }
